<div class="container">
 
    <div class="col-md-10">
      <h1>
        <%= @video.titulo %>
	  </source>
</video>
	<%= video_tag bscene.video.url(:medium), controls: true, style: "max-width: 100%;" %>
      </h1>
      <p class="small">
        <%= link_to 'Back to videos' ,videos_path %>
      </p>
      <% if @video.all_formats_encoded? %>
        <video class="video-js" data-setup="{ %>" height="264" id="my-video" preload="auto" width="640">
          <source src="<%= @video.mp4_file.url %>" type="video/mp4"></source>
          <source src="<%= @video.webm_file.url %>" type="video/webm"></source>
          <source src="<%= @video.ogg_file.url %>" type="video/ogg"></source>
	  <source src="<%= @video.MOV_file.url %>" type="video/MOV"></source>
	 <source src="<%= @video.m4v_file.url %>" type="video/m4v"></source>

          <p class="vjs-no-js">

          <p class="vjs-no-js">
            To view this video please enable JavaScript, and consider upgrading to a web browser that
            <a href="http://videojs.com/html5-video-support/" target="_blank">supports HTML5 video</a>
          </p>
        </video>
      <% else %>
        <p>Video is still being encoded.</p>
      <% end %>
    </div>
    <div class="col-md-2">
      <h1>
        <span class="likes-count">
          <%= @video.likes %>
        </span>
        <span class="glyphicon glyphicon-heart"></span>
      </h1>
    </div>
  </div>
</div>

